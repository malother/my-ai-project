import { Ionicons } from '@expo/vector-icons';
import { Pressable, Text, View } from 'react-native';
import { colors } from '../constants/theme';
import { Card, Header, Screen, styles } from '../components/ui';

export default function AccountSettingsScreen({ navigation }) { const rows = [{ icon: 'person-outline', label: 'Profile', value: 'Jordan Lee' }, { icon: 'wallet-outline', label: 'Bank accounts', value: '2 connected', route: 'BankAccounts' }, { icon: 'card-outline', label: 'Payment cards', value: '2 saved', route: 'ManageCards' }, { icon: 'notifications-outline', label: 'Notifications', value: 'On' }, { icon: 'lock-closed-outline', label: 'Security', value: 'Face ID' }]; return <Screen><Header title="Settings" subtitle="Your account and preferences" onPress={() => navigation.goBack()} icon="chevron-back" /><Card>{rows.map((row, index) => <Pressable key={row.label} onPress={() => row.route && navigation.navigate(row.route)} style={[{ flexDirection: 'row', alignItems: 'center', paddingVertical: 14 }, index > 0 && { borderTopWidth: 1, borderTopColor: colors.border }]}><View style={{ width: 35, height: 35, borderRadius: 11, backgroundColor: `${colors.teal}18`, alignItems: 'center', justifyContent: 'center' }}><Ionicons name={row.icon} size={18} color={colors.teal} /></View><Text style={{ color: colors.white, fontWeight: '700', flex: 1, marginLeft: 12 }}>{row.label}</Text><Text style={styles.subtitle}>{row.value}</Text><Ionicons name="chevron-forward" size={17} color={colors.subtle} style={{ marginLeft: 8 }} /></Pressable>)}</Card><Text style={{ color: colors.subtle, textAlign: 'center', fontSize: 12, marginTop: 28 }}>Family Budget · v1.0.0</Text></Screen>; }
